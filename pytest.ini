[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: tests that require external services or are slower (S3, Redis, DB, etc.)
    asyncio: async tests
asyncio_mode = auto
# Run coverage manually when needed:
# pytest --cov=src/des --cov-report=term-missing
